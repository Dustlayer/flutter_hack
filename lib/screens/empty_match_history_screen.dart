import 'package:flutter/material.dart';

class EmptyMatchHistoryScreen extends StatelessWidget {
  const EmptyMatchHistoryScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('Noch keine Matches vorhanden!');
  }
}
