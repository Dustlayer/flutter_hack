import 'package:flutter/material.dart';

class PlayScreen extends StatelessWidget {
  PlayScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return const Text("playscreen");
  }
}
